import React from 'react';
import { render } from '@testing-library/react';
import MonthsSwitcher from '../Expense/TimeRangeControl';

describe('Months Switcher', () => {
  it('should not show a prev and next button', () => {
    // const { getByTestId } = render(MonthsSwitcher);
  });

  it('should only show prev button', () => {});

  it('should show both buttons', () => {});

  it('should show only next button', () => {});
});
